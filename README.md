Sources for the plugin `plugin.appodeal.beta.AdColony`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.beta.AdColony" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
